---
phase: 01-assessment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - components.json
  - src/components/ui/slider.tsx
  - src/components/ui/card.tsx
  - src/components/ui/button.tsx
  - src/components/ui/progress.tsx
autonomous: true
requirements:
  - ASMT-01
must_haves:
  truths:
    - "npm run dev starts without errors"
    - "shadcn components.json exists in project root"
    - "Slider, Card, Button, Progress components exist in src/components/ui/"
    - "Tailwind v4 CSS loads on the page (no PostCSS config file needed)"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies declared"
      contains: "dexie"
    - path: "vite.config.ts"
      provides: "Vite 8 + Tailwind v4 plugin + React plugin config"
      contains: "tailwindcss"
    - path: "components.json"
      provides: "shadcn initialization gate"
      min_lines: 5
    - path: "src/components/ui/slider.tsx"
      provides: "shadcn Slider component"
  key_links:
    - from: "vite.config.ts"
      to: "src/index.css"
      via: "@import 'tailwindcss'"
      pattern: "tailwindcss"
---

<objective>
Scaffold the Peace Index project from scratch: initialize Vite 8 + React 19 + TypeScript, configure Tailwind CSS v4 via the @tailwindcss/vite plugin, initialize shadcn@canary, and add the four required shadcn components (Slider, Card, Button, Progress). Install all Phase 1 dependencies (Dexie, dexie-react-hooks, date-fns, lucide-react).

Purpose: Establish the complete project foundation before any feature code is written. The shadcn initialization gate (components.json) must exist before any component work can proceed.
Output: A running Vite dev server, all dependencies installed, shadcn initialized, and UI component files in src/components/ui/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-assessment/01-CONTEXT.md
@.planning/phases/01-assessment/01-RESEARCH.md
@.planning/phases/01-assessment/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project and install all Phase 1 dependencies</name>
  <read_first>
    - .planning/phases/01-assessment/01-RESEARCH.md (lines 82-122 — exact install commands and stack versions)
    - .planning/phases/01-assessment/01-UI-SPEC.md (lines 20-26 — shadcn gate note)
    - CLAUDE.md (lines 1-50 — project constraints)
  </read_first>
  <files>
    package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, index.html,
    src/main.tsx, src/App.tsx, src/index.css
  </files>
  <action>
Run ALL commands from the Peace_Index repo root directory (NOT inside a subdirectory). The project must be created in-place since the git repo already exists at C:\Users\Admin\Documents\Repos\Peace_Index.

Step 1 — Initialize Vite React+TS project in current directory:
```
npm create vite@latest . -- --template react-ts
```
When prompted about non-empty directory, select "Ignore files and continue".

Step 2 — Install Tailwind CSS v4:
```
npm install tailwindcss @tailwindcss/vite
```

Step 3 — Configure Tailwind in vite.config.ts. Replace the generated vite.config.ts content with:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [
    tailwindcss(),
    react(),
  ],
})
```

Step 4 — Update src/index.css. Replace ALL content with ONLY:
```css
@import "tailwindcss";
```
Do NOT include the default Vite CSS variables or styles — Tailwind v4 handles all base styles.

Step 5 — Initialize shadcn (CRITICAL GATE — must run before adding any components):
```
npx shadcn@canary init
```
When prompted:
- Style: Default
- Base color: Slate (matches UI-SPEC #F8FAFC/slate-50 secondary, #E2E8F0/slate-200 border)
- CSS variables: Yes

Step 6 — Add required shadcn components:
```
npx shadcn@canary add slider card button progress
```

Step 7 — Install remaining Phase 1 dependencies:
```
npm install dexie dexie-react-hooks
npm install date-fns lucide-react
```

Step 8 — Clean up default Vite boilerplate:
- Delete src/assets/react.svg if it exists
- Replace src/App.tsx content with a minimal placeholder:
```tsx
export default function App() {
  return (
    <div className="min-h-screen bg-white p-8">
      <h1 className="text-2xl font-semibold text-slate-900">Peace Index</h1>
      <p className="text-slate-500 mt-2">App scaffold ready.</p>
    </div>
  )
}
```
- Replace src/App.css content with empty string (or delete the file and remove its import from main.tsx)

Step 9 — Verify the dev server starts:
```
npm run dev
```
Confirm no errors in terminal output.
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); const deps={...pkg.dependencies,...pkg.devDependencies}; const required=['dexie','dexie-react-hooks','date-fns','lucide-react','tailwindcss']; const missing=required.filter(r=>!deps[r]); console.log(missing.length===0?'PASS: all deps present':'FAIL: missing '+missing.join(','))"
    </automated>
    <automated>
      node -e "const fs=require('fs'); console.log(fs.existsSync('components.json')?'PASS: components.json exists':'FAIL: components.json missing')"
    </automated>
    <automated>
      node -e "const fs=require('fs'); const files=['src/components/ui/slider.tsx','src/components/ui/card.tsx','src/components/ui/button.tsx','src/components/ui/progress.tsx']; const missing=files.filter(f=>!fs.existsSync(f)); console.log(missing.length===0?'PASS: all shadcn components present':'FAIL: missing '+missing.join(','))"
    </automated>
    <automated>
      node -e "const fs=require('fs'); const css=fs.readFileSync('src/index.css','utf8'); console.log(css.includes('@import \"tailwindcss\"')||css.includes(\"@import 'tailwindcss'\")?'PASS: Tailwind import present':'FAIL: Tailwind import missing from index.css')"
    </automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "dexie", "dexie-react-hooks", "date-fns", "lucide-react" in dependencies
    - package.json contains "tailwindcss", "@tailwindcss/vite" in devDependencies
    - components.json exists in project root
    - src/components/ui/slider.tsx exists
    - src/components/ui/card.tsx exists
    - src/components/ui/button.tsx exists
    - src/components/ui/progress.tsx exists
    - src/index.css contains exactly `@import "tailwindcss";` (no other base styles)
    - vite.config.ts imports and uses `tailwindcss` from `@tailwindcss/vite`
    - `npm run dev` exits with no errors (port binding is success; Ctrl+C to stop)
  </acceptance_criteria>
  <done>Project is fully scaffolded: Vite 8 + React 19 + TypeScript running, Tailwind v4 active, shadcn initialized with components.json, all four shadcn components present, and all Phase 1 npm dependencies installed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local disk | Package installation; supply chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm package install | accept | Greenfield scaffold using official Vite template and shadcn@canary official registry only. No third-party registries. Lock versions via package-lock.json generated by npm install. |
| T-01-02 | Information Disclosure | components.json | accept | No secrets in components.json — it is a config file with registry URLs and style preferences. Low-value target. |
</threat_model>

<verification>
Run the following after the task completes:

1. `node -e "require('./components.json')" && echo "PASS"` — components.json is valid JSON
2. `npm run build` — project builds without TypeScript or Vite errors
3. Open browser to http://localhost:5173 — see "Peace Index" heading on white background with Tailwind styling active
</verification>

<success_criteria>
- Vite dev server starts without error
- Browser loads the placeholder App with Tailwind-styled text
- All four shadcn components (slider, card, button, progress) exist in src/components/ui/
- All Phase 1 npm dependencies are installed (dexie, dexie-react-hooks, date-fns, lucide-react)
- components.json is present (shadcn gate cleared)
- package-lock.json is committed alongside package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-assessment/01-01-SUMMARY.md` using the summary template at @$HOME/.claude/get-shit-done/templates/summary.md.

Key items to record:
- Exact versions of react, vite, tailwindcss, dexie as installed (check package-lock.json)
- Any shadcn init prompts answered and settings chosen
- Whether `npm run build` passed without errors
- Path to vite.config.ts for downstream plans that need to reference it
</output>
